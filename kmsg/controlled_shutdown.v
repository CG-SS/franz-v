// Code generated by `v run generate/` from
// franz-go generate/definitions/07_controlled_shutdown — DO NOT EDIT.
module kmsg

import kbin

pub struct ControlledShutdownRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version      i16
	broker_id    int
	broker_epoch i64 = i64(-1)
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of ControlledShutdownRequest (7).
pub fn (m &ControlledShutdownRequest) key() i16 {
	return 7
}

// max_version returns the maximum wire version this client supports.
pub fn (m &ControlledShutdownRequest) max_version() i16 {
	return 3
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &ControlledShutdownRequest) is_flexible() bool {
	return m.version >= 3
}

// write_to encodes m at m.version into w.
pub fn (m &ControlledShutdownRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &ControlledShutdownRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.broker_id)
	if version >= 2 {
		w.write_int64(m.broker_epoch)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = ControlledShutdownRequest{version: v}).
pub fn (mut m ControlledShutdownRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m ControlledShutdownRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	m.broker_id = r.read_int32()
	if version >= 2 {
		m.broker_epoch = r.read_int64()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct ControlledShutdownResponsePartitionsRemaining {
pub mut:
	topic     string
	partition int
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &ControlledShutdownResponsePartitionsRemaining) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.topic)
	} else {
		w.write_string(m.topic)
	}
	w.write_int32(m.partition)
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m ControlledShutdownResponsePartitionsRemaining) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.topic = r.read_compact_string()
	} else {
		m.topic = r.read_string()
	}
	m.partition = r.read_int32()
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct ControlledShutdownResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version              i16
	error_code           i16
	partitions_remaining []ControlledShutdownResponsePartitionsRemaining
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of ControlledShutdownResponse (7).
pub fn (m &ControlledShutdownResponse) key() i16 {
	return 7
}

// max_version returns the maximum wire version this client supports.
pub fn (m &ControlledShutdownResponse) max_version() i16 {
	return 3
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &ControlledShutdownResponse) is_flexible() bool {
	return m.version >= 3
}

// write_to encodes m at m.version into w.
pub fn (m &ControlledShutdownResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &ControlledShutdownResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int16(m.error_code)
	arr_partitions_remaining := m.partitions_remaining
	if flexible {
		w.write_compact_array_len(arr_partitions_remaining.len)
	} else {
		w.write_array_len(arr_partitions_remaining.len)
	}
	for item_partitions_remaining in arr_partitions_remaining {
		item_partitions_remaining.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = ControlledShutdownResponse{version: v}).
pub fn (mut m ControlledShutdownResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m ControlledShutdownResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.error_code = r.read_int16()
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_partitions_remaining := []ControlledShutdownResponsePartitionsRemaining{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_partitions_remaining := ControlledShutdownResponsePartitionsRemaining{}
		item_partitions_remaining.read_from_versioned(mut r, version, flexible)!
		arr_partitions_remaining << item_partitions_remaining
	}
	m.partitions_remaining = arr_partitions_remaining
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
