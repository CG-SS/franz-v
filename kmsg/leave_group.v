// Code generated by `v run generate/` from
// franz-go generate/definitions/13_leave_group — DO NOT EDIT.
module kmsg

import kbin

pub struct LeaveGroupRequestMember {
pub mut:
	member_id   string
	instance_id ?string
	reason      ?string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &LeaveGroupRequestMember) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.member_id)
	} else {
		w.write_string(m.member_id)
	}
	if flexible {
		w.write_compact_nullable_string(m.instance_id)
	} else {
		w.write_nullable_string(m.instance_id)
	}
	if version >= 5 {
		if flexible {
			w.write_compact_nullable_string(m.reason)
		} else {
			w.write_nullable_string(m.reason)
		}
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m LeaveGroupRequestMember) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.member_id = r.read_compact_string()
	} else {
		m.member_id = r.read_string()
	}
	if flexible {
		m.instance_id = r.read_compact_nullable_string()
	} else {
		m.instance_id = r.read_nullable_string()
	}
	if version >= 5 {
		if flexible {
			m.reason = r.read_compact_nullable_string()
		} else {
			m.reason = r.read_nullable_string()
		}
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct LeaveGroupRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version   i16
	group     string
	member_id string
	members   []LeaveGroupRequestMember
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of LeaveGroupRequest (13).
pub fn (m &LeaveGroupRequest) key() i16 {
	return 13
}

// max_version returns the maximum wire version this client supports.
pub fn (m &LeaveGroupRequest) max_version() i16 {
	return 5
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &LeaveGroupRequest) is_flexible() bool {
	return m.version >= 4
}

// write_to encodes m at m.version into w.
pub fn (m &LeaveGroupRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &LeaveGroupRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.group)
	} else {
		w.write_string(m.group)
	}
	if version <= 2 {
		if flexible {
			w.write_compact_string(m.member_id)
		} else {
			w.write_string(m.member_id)
		}
	}
	if version >= 3 {
		arr_members := m.members
		if flexible {
			w.write_compact_array_len(arr_members.len)
		} else {
			w.write_array_len(arr_members.len)
		}
		for item_members in arr_members {
			item_members.write_to_versioned(mut w, version, flexible)
		}
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = LeaveGroupRequest{version: v}).
pub fn (mut m LeaveGroupRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m LeaveGroupRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if flexible {
		m.group = r.read_compact_string()
	} else {
		m.group = r.read_string()
	}
	if version <= 2 {
		if flexible {
			m.member_id = r.read_compact_string()
		} else {
			m.member_id = r.read_string()
		}
	}
	if version >= 3 {
		if flexible {
			arr_len = r.read_compact_array_len()
		} else {
			arr_len = r.read_array_len()
		}
		mut arr_members := []LeaveGroupRequestMember{cap: if arr_len > 0 { arr_len } else { 0 }}
		for _ in 0 .. arr_len {
			mut item_members := LeaveGroupRequestMember{}
			item_members.read_from_versioned(mut r, version, flexible)!
			arr_members << item_members
		}
		m.members = arr_members
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct LeaveGroupResponseMember {
pub mut:
	member_id   string
	instance_id ?string
	error_code  i16
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &LeaveGroupResponseMember) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.member_id)
	} else {
		w.write_string(m.member_id)
	}
	if flexible {
		w.write_compact_nullable_string(m.instance_id)
	} else {
		w.write_nullable_string(m.instance_id)
	}
	w.write_int16(m.error_code)
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m LeaveGroupResponseMember) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.member_id = r.read_compact_string()
	} else {
		m.member_id = r.read_string()
	}
	if flexible {
		m.instance_id = r.read_compact_nullable_string()
	} else {
		m.instance_id = r.read_nullable_string()
	}
	m.error_code = r.read_int16()
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct LeaveGroupResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version         i16
	throttle_millis int
	error_code      i16
	members         []LeaveGroupResponseMember
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of LeaveGroupResponse (13).
pub fn (m &LeaveGroupResponse) key() i16 {
	return 13
}

// max_version returns the maximum wire version this client supports.
pub fn (m &LeaveGroupResponse) max_version() i16 {
	return 5
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &LeaveGroupResponse) is_flexible() bool {
	return m.version >= 4
}

// write_to encodes m at m.version into w.
pub fn (m &LeaveGroupResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &LeaveGroupResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if version >= 1 {
		w.write_int32(m.throttle_millis)
	}
	w.write_int16(m.error_code)
	if version >= 3 {
		arr_members := m.members
		if flexible {
			w.write_compact_array_len(arr_members.len)
		} else {
			w.write_array_len(arr_members.len)
		}
		for item_members in arr_members {
			item_members.write_to_versioned(mut w, version, flexible)
		}
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = LeaveGroupResponse{version: v}).
pub fn (mut m LeaveGroupResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m LeaveGroupResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if version >= 1 {
		m.throttle_millis = r.read_int32()
	}
	m.error_code = r.read_int16()
	if version >= 3 {
		if flexible {
			arr_len = r.read_compact_array_len()
		} else {
			arr_len = r.read_array_len()
		}
		mut arr_members := []LeaveGroupResponseMember{cap: if arr_len > 0 { arr_len } else { 0 }}
		for _ in 0 .. arr_len {
			mut item_members := LeaveGroupResponseMember{}
			item_members.read_from_versioned(mut r, version, flexible)!
			arr_members << item_members
		}
		m.members = arr_members
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
