// Code generated by `v run generate/` from
// franz-go generate/definitions/17_sasl_handshake — DO NOT EDIT.
module kmsg

import kbin

pub struct SASLHandshakeRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version   i16
	mechanism string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of SASLHandshakeRequest (17).
pub fn (m &SASLHandshakeRequest) key() i16 {
	return 17
}

// max_version returns the maximum wire version this client supports.
pub fn (m &SASLHandshakeRequest) max_version() i16 {
	return 1
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &SASLHandshakeRequest) is_flexible() bool {
	return false
}

// write_to encodes m at m.version into w.
pub fn (m &SASLHandshakeRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &SASLHandshakeRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.mechanism)
	} else {
		w.write_string(m.mechanism)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = SASLHandshakeRequest{version: v}).
pub fn (mut m SASLHandshakeRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m SASLHandshakeRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.mechanism = r.read_compact_string()
	} else {
		m.mechanism = r.read_string()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct SASLHandshakeResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version              i16
	error_code           i16
	supported_mechanisms []string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of SASLHandshakeResponse (17).
pub fn (m &SASLHandshakeResponse) key() i16 {
	return 17
}

// max_version returns the maximum wire version this client supports.
pub fn (m &SASLHandshakeResponse) max_version() i16 {
	return 1
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &SASLHandshakeResponse) is_flexible() bool {
	return false
}

// write_to encodes m at m.version into w.
pub fn (m &SASLHandshakeResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &SASLHandshakeResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int16(m.error_code)
	arr_supported_mechanisms := m.supported_mechanisms
	if flexible {
		w.write_compact_array_len(arr_supported_mechanisms.len)
	} else {
		w.write_array_len(arr_supported_mechanisms.len)
	}
	for item_supported_mechanisms in arr_supported_mechanisms {
		if flexible {
			w.write_compact_string(item_supported_mechanisms)
		} else {
			w.write_string(item_supported_mechanisms)
		}
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = SASLHandshakeResponse{version: v}).
pub fn (mut m SASLHandshakeResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m SASLHandshakeResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.error_code = r.read_int16()
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_supported_mechanisms := []string{cap: if arr_len > 0 { arr_len } else { 0 }}
	for _ in 0 .. arr_len {
		mut item_supported_mechanisms := ''
		if flexible {
			item_supported_mechanisms = r.read_compact_string()
		} else {
			item_supported_mechanisms = r.read_string()
		}
		arr_supported_mechanisms << item_supported_mechanisms
	}
	m.supported_mechanisms = arr_supported_mechanisms
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
