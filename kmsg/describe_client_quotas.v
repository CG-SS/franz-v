// Code generated by `v run generate/` from
// franz-go generate/definitions/48_describe_client_quotas — DO NOT EDIT.
module kmsg

import kbin

pub struct DescribeClientQuotasRequestComponent {
pub mut:
	entity_type string
	match_type  QuotasMatchType
	match_      ?string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeClientQuotasRequestComponent) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.entity_type)
	} else {
		w.write_string(m.entity_type)
	}
	w.write_int8(i8(m.match_type))
	if flexible {
		w.write_compact_nullable_string(m.match_)
	} else {
		w.write_nullable_string(m.match_)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeClientQuotasRequestComponent) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.entity_type = r.read_compact_string()
	} else {
		m.entity_type = r.read_string()
	}
	m.match_type = QuotasMatchType(r.read_int8())
	if flexible {
		m.match_ = r.read_compact_nullable_string()
	} else {
		m.match_ = r.read_nullable_string()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeClientQuotasRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version    i16
	components []DescribeClientQuotasRequestComponent
	strict     bool
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeClientQuotasRequest (48).
pub fn (m &DescribeClientQuotasRequest) key() i16 {
	return 48
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeClientQuotasRequest) max_version() i16 {
	return 1
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeClientQuotasRequest) is_flexible() bool {
	return m.version >= 1
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeClientQuotasRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeClientQuotasRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	arr_components := m.components
	if flexible {
		w.write_compact_array_len(arr_components.len)
	} else {
		w.write_array_len(arr_components.len)
	}
	for item_components in arr_components {
		item_components.write_to_versioned(mut w, version, flexible)
	}
	w.write_bool(m.strict)
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeClientQuotasRequest{version: v}).
pub fn (mut m DescribeClientQuotasRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeClientQuotasRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_components := []DescribeClientQuotasRequestComponent{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_components := DescribeClientQuotasRequestComponent{}
		item_components.read_from_versioned(mut r, version, flexible)!
		arr_components << item_components
	}
	m.components = arr_components
	m.strict = r.read_bool()
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeClientQuotasResponseEntryEntity {
pub mut:
	type_ string
	name  ?string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeClientQuotasResponseEntryEntity) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.type_)
	} else {
		w.write_string(m.type_)
	}
	if flexible {
		w.write_compact_nullable_string(m.name)
	} else {
		w.write_nullable_string(m.name)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeClientQuotasResponseEntryEntity) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.type_ = r.read_compact_string()
	} else {
		m.type_ = r.read_string()
	}
	if flexible {
		m.name = r.read_compact_nullable_string()
	} else {
		m.name = r.read_nullable_string()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeClientQuotasResponseEntryValue {
pub mut:
	key   string
	value f64
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeClientQuotasResponseEntryValue) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.key)
	} else {
		w.write_string(m.key)
	}
	w.write_float64(m.value)
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeClientQuotasResponseEntryValue) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.key = r.read_compact_string()
	} else {
		m.key = r.read_string()
	}
	m.value = r.read_float64()
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeClientQuotasResponseEntry {
pub mut:
	entity []DescribeClientQuotasResponseEntryEntity
	values []DescribeClientQuotasResponseEntryValue
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeClientQuotasResponseEntry) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	arr_entity := m.entity
	if flexible {
		w.write_compact_array_len(arr_entity.len)
	} else {
		w.write_array_len(arr_entity.len)
	}
	for item_entity in arr_entity {
		item_entity.write_to_versioned(mut w, version, flexible)
	}
	arr_values := m.values
	if flexible {
		w.write_compact_array_len(arr_values.len)
	} else {
		w.write_array_len(arr_values.len)
	}
	for item_values in arr_values {
		item_values.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeClientQuotasResponseEntry) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_entity := []DescribeClientQuotasResponseEntryEntity{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_entity := DescribeClientQuotasResponseEntryEntity{}
		item_entity.read_from_versioned(mut r, version, flexible)!
		arr_entity << item_entity
	}
	m.entity = arr_entity
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_values := []DescribeClientQuotasResponseEntryValue{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_values := DescribeClientQuotasResponseEntryValue{}
		item_values.read_from_versioned(mut r, version, flexible)!
		arr_values << item_values
	}
	m.values = arr_values
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeClientQuotasResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version         i16
	throttle_millis int
	error_code      i16
	error_message   ?string
	entries         ?[]DescribeClientQuotasResponseEntry
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeClientQuotasResponse (48).
pub fn (m &DescribeClientQuotasResponse) key() i16 {
	return 48
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeClientQuotasResponse) max_version() i16 {
	return 1
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeClientQuotasResponse) is_flexible() bool {
	return m.version >= 1
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeClientQuotasResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeClientQuotasResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.throttle_millis)
	w.write_int16(m.error_code)
	if flexible {
		w.write_compact_nullable_string(m.error_message)
	} else {
		w.write_nullable_string(m.error_message)
	}
	if arr_entries := m.entries {
		if flexible {
			w.write_compact_array_len(arr_entries.len)
		} else {
			w.write_array_len(arr_entries.len)
		}
		for item_entries in arr_entries {
			item_entries.write_to_versioned(mut w, version, flexible)
		}
	} else {
		if flexible {
			w.write_uvarint(0)
		} else {
			w.write_int32(-1)
		}
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeClientQuotasResponse{version: v}).
pub fn (mut m DescribeClientQuotasResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeClientQuotasResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.throttle_millis = r.read_int32()
	m.error_code = r.read_int16()
	if flexible {
		m.error_message = r.read_compact_nullable_string()
	} else {
		m.error_message = r.read_nullable_string()
	}
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	if arr_len < 0 {
		m.entries = none
	} else {
		mut arr_entries := []DescribeClientQuotasResponseEntry{cap: if arr_len > 0 {
			arr_len
		} else {
			0
		}}
		for _ in 0 .. arr_len {
			mut item_entries := DescribeClientQuotasResponseEntry{}
			item_entries.read_from_versioned(mut r, version, flexible)!
			arr_entries << item_entries
		}
		m.entries = arr_entries
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
