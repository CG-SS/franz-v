// Code generated by `v run generate/` from
// franz-go generate/definitions/38_create_delegation_token — DO NOT EDIT.
module kmsg

import kbin

pub struct CreateDelegationTokenRequestRenewer {
pub mut:
	principal_type string
	principal_name string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &CreateDelegationTokenRequestRenewer) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.principal_type)
	} else {
		w.write_string(m.principal_type)
	}
	if flexible {
		w.write_compact_string(m.principal_name)
	} else {
		w.write_string(m.principal_name)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m CreateDelegationTokenRequestRenewer) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.principal_type = r.read_compact_string()
	} else {
		m.principal_type = r.read_string()
	}
	if flexible {
		m.principal_name = r.read_compact_string()
	} else {
		m.principal_name = r.read_string()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct CreateDelegationTokenRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version              i16
	owner_principal_type ?string
	owner_principal_name ?string
	renewers             []CreateDelegationTokenRequestRenewer
	max_lifetime_millis  i64
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of CreateDelegationTokenRequest (38).
pub fn (m &CreateDelegationTokenRequest) key() i16 {
	return 38
}

// max_version returns the maximum wire version this client supports.
pub fn (m &CreateDelegationTokenRequest) max_version() i16 {
	return 3
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &CreateDelegationTokenRequest) is_flexible() bool {
	return m.version >= 2
}

// write_to encodes m at m.version into w.
pub fn (m &CreateDelegationTokenRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &CreateDelegationTokenRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if version >= 3 {
		if flexible {
			w.write_compact_nullable_string(m.owner_principal_type)
		} else {
			w.write_nullable_string(m.owner_principal_type)
		}
	}
	if version >= 3 {
		if flexible {
			w.write_compact_nullable_string(m.owner_principal_name)
		} else {
			w.write_nullable_string(m.owner_principal_name)
		}
	}
	arr_renewers := m.renewers
	if flexible {
		w.write_compact_array_len(arr_renewers.len)
	} else {
		w.write_array_len(arr_renewers.len)
	}
	for item_renewers in arr_renewers {
		item_renewers.write_to_versioned(mut w, version, flexible)
	}
	w.write_int64(m.max_lifetime_millis)
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = CreateDelegationTokenRequest{version: v}).
pub fn (mut m CreateDelegationTokenRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m CreateDelegationTokenRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if version >= 3 {
		if flexible {
			m.owner_principal_type = r.read_compact_nullable_string()
		} else {
			m.owner_principal_type = r.read_nullable_string()
		}
	}
	if version >= 3 {
		if flexible {
			m.owner_principal_name = r.read_compact_nullable_string()
		} else {
			m.owner_principal_name = r.read_nullable_string()
		}
	}
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_renewers := []CreateDelegationTokenRequestRenewer{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_renewers := CreateDelegationTokenRequestRenewer{}
		item_renewers.read_from_versioned(mut r, version, flexible)!
		arr_renewers << item_renewers
	}
	m.renewers = arr_renewers
	m.max_lifetime_millis = r.read_int64()
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct CreateDelegationTokenResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version                        i16
	error_code                     i16
	principal_type                 string
	principal_name                 string
	token_requester_principal_type string
	token_requester_principal_name string
	issue_timestamp                i64
	expiry_timestamp               i64
	max_timestamp                  i64
	token_id                       string
	hmac                           []u8
	throttle_millis                int
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of CreateDelegationTokenResponse (38).
pub fn (m &CreateDelegationTokenResponse) key() i16 {
	return 38
}

// max_version returns the maximum wire version this client supports.
pub fn (m &CreateDelegationTokenResponse) max_version() i16 {
	return 3
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &CreateDelegationTokenResponse) is_flexible() bool {
	return m.version >= 2
}

// write_to encodes m at m.version into w.
pub fn (m &CreateDelegationTokenResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &CreateDelegationTokenResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int16(m.error_code)
	if flexible {
		w.write_compact_string(m.principal_type)
	} else {
		w.write_string(m.principal_type)
	}
	if flexible {
		w.write_compact_string(m.principal_name)
	} else {
		w.write_string(m.principal_name)
	}
	if version >= 3 {
		if flexible {
			w.write_compact_string(m.token_requester_principal_type)
		} else {
			w.write_string(m.token_requester_principal_type)
		}
	}
	if version >= 3 {
		if flexible {
			w.write_compact_string(m.token_requester_principal_name)
		} else {
			w.write_string(m.token_requester_principal_name)
		}
	}
	w.write_int64(m.issue_timestamp)
	w.write_int64(m.expiry_timestamp)
	w.write_int64(m.max_timestamp)
	if flexible {
		w.write_compact_string(m.token_id)
	} else {
		w.write_string(m.token_id)
	}
	if flexible {
		w.write_compact_bytes(m.hmac)
	} else {
		w.write_bytes(m.hmac)
	}
	w.write_int32(m.throttle_millis)
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = CreateDelegationTokenResponse{version: v}).
pub fn (mut m CreateDelegationTokenResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m CreateDelegationTokenResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	m.error_code = r.read_int16()
	if flexible {
		m.principal_type = r.read_compact_string()
	} else {
		m.principal_type = r.read_string()
	}
	if flexible {
		m.principal_name = r.read_compact_string()
	} else {
		m.principal_name = r.read_string()
	}
	if version >= 3 {
		if flexible {
			m.token_requester_principal_type = r.read_compact_string()
		} else {
			m.token_requester_principal_type = r.read_string()
		}
	}
	if version >= 3 {
		if flexible {
			m.token_requester_principal_name = r.read_compact_string()
		} else {
			m.token_requester_principal_name = r.read_string()
		}
	}
	m.issue_timestamp = r.read_int64()
	m.expiry_timestamp = r.read_int64()
	m.max_timestamp = r.read_int64()
	if flexible {
		m.token_id = r.read_compact_string()
	} else {
		m.token_id = r.read_string()
	}
	if flexible {
		m.hmac = r.read_compact_bytes()
	} else {
		m.hmac = r.read_bytes()
	}
	m.throttle_millis = r.read_int32()
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
