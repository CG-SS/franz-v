// Code generated by `v run generate/` from
// franz-go generate/definitions/06_update_metadata — DO NOT EDIT.
module kmsg

import kbin

pub struct UpdateMetadataRequestTopicPartition {
pub mut:
	topic            string
	partition        int
	controller_epoch int
	leader           int
	leader_epoch     int
	isr              []int
	zk_version       int
	replicas         []int
	offline_replicas []int
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &UpdateMetadataRequestTopicPartition) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if version <= 4 {
		if flexible {
			w.write_compact_string(m.topic)
		} else {
			w.write_string(m.topic)
		}
	}
	w.write_int32(m.partition)
	w.write_int32(m.controller_epoch)
	w.write_int32(m.leader)
	w.write_int32(m.leader_epoch)
	arr_isr := m.isr
	if flexible {
		w.write_compact_array_len(arr_isr.len)
	} else {
		w.write_array_len(arr_isr.len)
	}
	for item_isr in arr_isr {
		w.write_int32(item_isr)
	}
	w.write_int32(m.zk_version)
	arr_replicas := m.replicas
	if flexible {
		w.write_compact_array_len(arr_replicas.len)
	} else {
		w.write_array_len(arr_replicas.len)
	}
	for item_replicas in arr_replicas {
		w.write_int32(item_replicas)
	}
	if version >= 4 {
		arr_offline_replicas := m.offline_replicas
		if flexible {
			w.write_compact_array_len(arr_offline_replicas.len)
		} else {
			w.write_array_len(arr_offline_replicas.len)
		}
		for item_offline_replicas in arr_offline_replicas {
			w.write_int32(item_offline_replicas)
		}
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m UpdateMetadataRequestTopicPartition) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if version <= 4 {
		if flexible {
			m.topic = r.read_compact_string()
		} else {
			m.topic = r.read_string()
		}
	}
	m.partition = r.read_int32()
	m.controller_epoch = r.read_int32()
	m.leader = r.read_int32()
	m.leader_epoch = r.read_int32()
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_isr := []int{cap: if arr_len > 0 { arr_len } else { 0 }}
	for _ in 0 .. arr_len {
		mut item_isr := 0
		item_isr = r.read_int32()
		arr_isr << item_isr
	}
	m.isr = arr_isr
	m.zk_version = r.read_int32()
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_replicas := []int{cap: if arr_len > 0 { arr_len } else { 0 }}
	for _ in 0 .. arr_len {
		mut item_replicas := 0
		item_replicas = r.read_int32()
		arr_replicas << item_replicas
	}
	m.replicas = arr_replicas
	if version >= 4 {
		if flexible {
			arr_len = r.read_compact_array_len()
		} else {
			arr_len = r.read_array_len()
		}
		mut arr_offline_replicas := []int{cap: if arr_len > 0 { arr_len } else { 0 }}
		for _ in 0 .. arr_len {
			mut item_offline_replicas := 0
			item_offline_replicas = r.read_int32()
			arr_offline_replicas << item_offline_replicas
		}
		m.offline_replicas = arr_offline_replicas
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct UpdateMetadataRequestTopicState {
pub mut:
	topic            string
	topic_id         [16]u8
	partition_states []UpdateMetadataRequestTopicPartition
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &UpdateMetadataRequestTopicState) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.topic)
	} else {
		w.write_string(m.topic)
	}
	if version >= 7 {
		w.write_uuid(m.topic_id)
	}
	arr_partition_states := m.partition_states
	if flexible {
		w.write_compact_array_len(arr_partition_states.len)
	} else {
		w.write_array_len(arr_partition_states.len)
	}
	for item_partition_states in arr_partition_states {
		item_partition_states.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m UpdateMetadataRequestTopicState) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if flexible {
		m.topic = r.read_compact_string()
	} else {
		m.topic = r.read_string()
	}
	if version >= 7 {
		m.topic_id = r.read_uuid()
	}
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_partition_states := []UpdateMetadataRequestTopicPartition{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_partition_states := UpdateMetadataRequestTopicPartition{}
		item_partition_states.read_from_versioned(mut r, version, flexible)!
		arr_partition_states << item_partition_states
	}
	m.partition_states = arr_partition_states
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct UpdateMetadataRequestLiveBrokerEndpoint {
pub mut:
	port              int
	host              string
	listener_name     string
	security_protocol i16
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &UpdateMetadataRequestLiveBrokerEndpoint) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.port)
	if flexible {
		w.write_compact_string(m.host)
	} else {
		w.write_string(m.host)
	}
	if version >= 3 {
		if flexible {
			w.write_compact_string(m.listener_name)
		} else {
			w.write_string(m.listener_name)
		}
	}
	w.write_int16(m.security_protocol)
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m UpdateMetadataRequestLiveBrokerEndpoint) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	m.port = r.read_int32()
	if flexible {
		m.host = r.read_compact_string()
	} else {
		m.host = r.read_string()
	}
	if version >= 3 {
		if flexible {
			m.listener_name = r.read_compact_string()
		} else {
			m.listener_name = r.read_string()
		}
	}
	m.security_protocol = r.read_int16()
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct UpdateMetadataRequestLiveBroker {
pub mut:
	id        int
	host      string
	port      int
	endpoints []UpdateMetadataRequestLiveBrokerEndpoint
	rack      ?string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &UpdateMetadataRequestLiveBroker) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.id)
	if version <= 0 {
		if flexible {
			w.write_compact_string(m.host)
		} else {
			w.write_string(m.host)
		}
	}
	if version <= 0 {
		w.write_int32(m.port)
	}
	if version >= 1 {
		arr_endpoints := m.endpoints
		if flexible {
			w.write_compact_array_len(arr_endpoints.len)
		} else {
			w.write_array_len(arr_endpoints.len)
		}
		for item_endpoints in arr_endpoints {
			item_endpoints.write_to_versioned(mut w, version, flexible)
		}
	}
	if version >= 2 {
		if flexible {
			w.write_compact_nullable_string(m.rack)
		} else {
			w.write_nullable_string(m.rack)
		}
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m UpdateMetadataRequestLiveBroker) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.id = r.read_int32()
	if version <= 0 {
		if flexible {
			m.host = r.read_compact_string()
		} else {
			m.host = r.read_string()
		}
	}
	if version <= 0 {
		m.port = r.read_int32()
	}
	if version >= 1 {
		if flexible {
			arr_len = r.read_compact_array_len()
		} else {
			arr_len = r.read_array_len()
		}
		mut arr_endpoints := []UpdateMetadataRequestLiveBrokerEndpoint{cap: if arr_len > 0 {
			arr_len
		} else {
			0
		}}
		for _ in 0 .. arr_len {
			mut item_endpoints := UpdateMetadataRequestLiveBrokerEndpoint{}
			item_endpoints.read_from_versioned(mut r, version, flexible)!
			arr_endpoints << item_endpoints
		}
		m.endpoints = arr_endpoints
	}
	if version >= 2 {
		if flexible {
			m.rack = r.read_compact_nullable_string()
		} else {
			m.rack = r.read_nullable_string()
		}
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct UpdateMetadataRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version              i16
	controller_id        int
	is_k_raft_controller bool
	controller_epoch     int
	broker_epoch         i64 = i64(-1)
	partition_states     []UpdateMetadataRequestTopicPartition
	topic_states         []UpdateMetadataRequestTopicState
	live_brokers         []UpdateMetadataRequestLiveBroker
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of UpdateMetadataRequest (6).
pub fn (m &UpdateMetadataRequest) key() i16 {
	return 6
}

// max_version returns the maximum wire version this client supports.
pub fn (m &UpdateMetadataRequest) max_version() i16 {
	return 8
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &UpdateMetadataRequest) is_flexible() bool {
	return m.version >= 6
}

// write_to encodes m at m.version into w.
pub fn (m &UpdateMetadataRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &UpdateMetadataRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.controller_id)
	if version >= 8 {
		w.write_bool(m.is_k_raft_controller)
	}
	w.write_int32(m.controller_epoch)
	if version >= 5 {
		w.write_int64(m.broker_epoch)
	}
	if version <= 4 {
		arr_partition_states := m.partition_states
		if flexible {
			w.write_compact_array_len(arr_partition_states.len)
		} else {
			w.write_array_len(arr_partition_states.len)
		}
		for item_partition_states in arr_partition_states {
			item_partition_states.write_to_versioned(mut w, version, flexible)
		}
	}
	if version >= 5 {
		arr_topic_states := m.topic_states
		if flexible {
			w.write_compact_array_len(arr_topic_states.len)
		} else {
			w.write_array_len(arr_topic_states.len)
		}
		for item_topic_states in arr_topic_states {
			item_topic_states.write_to_versioned(mut w, version, flexible)
		}
	}
	arr_live_brokers := m.live_brokers
	if flexible {
		w.write_compact_array_len(arr_live_brokers.len)
	} else {
		w.write_array_len(arr_live_brokers.len)
	}
	for item_live_brokers in arr_live_brokers {
		item_live_brokers.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = UpdateMetadataRequest{version: v}).
pub fn (mut m UpdateMetadataRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m UpdateMetadataRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.controller_id = r.read_int32()
	if version >= 8 {
		m.is_k_raft_controller = r.read_bool()
	}
	m.controller_epoch = r.read_int32()
	if version >= 5 {
		m.broker_epoch = r.read_int64()
	}
	if version <= 4 {
		if flexible {
			arr_len = r.read_compact_array_len()
		} else {
			arr_len = r.read_array_len()
		}
		mut arr_partition_states := []UpdateMetadataRequestTopicPartition{cap: if arr_len > 0 {
			arr_len
		} else {
			0
		}}
		for _ in 0 .. arr_len {
			mut item_partition_states := UpdateMetadataRequestTopicPartition{}
			item_partition_states.read_from_versioned(mut r, version, flexible)!
			arr_partition_states << item_partition_states
		}
		m.partition_states = arr_partition_states
	}
	if version >= 5 {
		if flexible {
			arr_len = r.read_compact_array_len()
		} else {
			arr_len = r.read_array_len()
		}
		mut arr_topic_states := []UpdateMetadataRequestTopicState{cap: if arr_len > 0 {
			arr_len
		} else {
			0
		}}
		for _ in 0 .. arr_len {
			mut item_topic_states := UpdateMetadataRequestTopicState{}
			item_topic_states.read_from_versioned(mut r, version, flexible)!
			arr_topic_states << item_topic_states
		}
		m.topic_states = arr_topic_states
	}
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_live_brokers := []UpdateMetadataRequestLiveBroker{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_live_brokers := UpdateMetadataRequestLiveBroker{}
		item_live_brokers.read_from_versioned(mut r, version, flexible)!
		arr_live_brokers << item_live_brokers
	}
	m.live_brokers = arr_live_brokers
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct UpdateMetadataResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version    i16
	error_code i16
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of UpdateMetadataResponse (6).
pub fn (m &UpdateMetadataResponse) key() i16 {
	return 6
}

// max_version returns the maximum wire version this client supports.
pub fn (m &UpdateMetadataResponse) max_version() i16 {
	return 8
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &UpdateMetadataResponse) is_flexible() bool {
	return m.version >= 6
}

// write_to encodes m at m.version into w.
pub fn (m &UpdateMetadataResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &UpdateMetadataResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int16(m.error_code)
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = UpdateMetadataResponse{version: v}).
pub fn (mut m UpdateMetadataResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m UpdateMetadataResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	m.error_code = r.read_int16()
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
