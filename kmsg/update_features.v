// Code generated by `v run generate/` from
// franz-go generate/definitions/57_update_features — DO NOT EDIT.
module kmsg

import kbin

pub struct UpdateFeaturesRequestFeatureUpdate {
pub mut:
	feature           string
	max_version_level i16
	allow_downgrade   bool
	upgrade_type      i8
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &UpdateFeaturesRequestFeatureUpdate) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.feature)
	} else {
		w.write_string(m.feature)
	}
	w.write_int16(m.max_version_level)
	if version <= 0 {
		w.write_bool(m.allow_downgrade)
	}
	if version >= 1 {
		w.write_int8(m.upgrade_type)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m UpdateFeaturesRequestFeatureUpdate) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.feature = r.read_compact_string()
	} else {
		m.feature = r.read_string()
	}
	m.max_version_level = r.read_int16()
	if version <= 0 {
		m.allow_downgrade = r.read_bool()
	}
	if version >= 1 {
		m.upgrade_type = r.read_int8()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct UpdateFeaturesRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version         i16
	timeout_millis  int = 15000
	feature_updates []UpdateFeaturesRequestFeatureUpdate
	validate_only   bool
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of UpdateFeaturesRequest (57).
pub fn (m &UpdateFeaturesRequest) key() i16 {
	return 57
}

// max_version returns the maximum wire version this client supports.
pub fn (m &UpdateFeaturesRequest) max_version() i16 {
	return 2
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &UpdateFeaturesRequest) is_flexible() bool {
	return m.version >= 0
}

// write_to encodes m at m.version into w.
pub fn (m &UpdateFeaturesRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &UpdateFeaturesRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.timeout_millis)
	arr_feature_updates := m.feature_updates
	if flexible {
		w.write_compact_array_len(arr_feature_updates.len)
	} else {
		w.write_array_len(arr_feature_updates.len)
	}
	for item_feature_updates in arr_feature_updates {
		item_feature_updates.write_to_versioned(mut w, version, flexible)
	}
	if version >= 1 {
		w.write_bool(m.validate_only)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = UpdateFeaturesRequest{version: v}).
pub fn (mut m UpdateFeaturesRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m UpdateFeaturesRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.timeout_millis = r.read_int32()
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_feature_updates := []UpdateFeaturesRequestFeatureUpdate{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_feature_updates := UpdateFeaturesRequestFeatureUpdate{}
		item_feature_updates.read_from_versioned(mut r, version, flexible)!
		arr_feature_updates << item_feature_updates
	}
	m.feature_updates = arr_feature_updates
	if version >= 1 {
		m.validate_only = r.read_bool()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct UpdateFeaturesResponseResult {
pub mut:
	feature       string
	error_code    i16
	error_message ?string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &UpdateFeaturesResponseResult) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.feature)
	} else {
		w.write_string(m.feature)
	}
	w.write_int16(m.error_code)
	if flexible {
		w.write_compact_nullable_string(m.error_message)
	} else {
		w.write_nullable_string(m.error_message)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m UpdateFeaturesResponseResult) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.feature = r.read_compact_string()
	} else {
		m.feature = r.read_string()
	}
	m.error_code = r.read_int16()
	if flexible {
		m.error_message = r.read_compact_nullable_string()
	} else {
		m.error_message = r.read_nullable_string()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct UpdateFeaturesResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version         i16
	throttle_millis int
	error_code      i16
	error_message   ?string
	results         []UpdateFeaturesResponseResult
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of UpdateFeaturesResponse (57).
pub fn (m &UpdateFeaturesResponse) key() i16 {
	return 57
}

// max_version returns the maximum wire version this client supports.
pub fn (m &UpdateFeaturesResponse) max_version() i16 {
	return 2
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &UpdateFeaturesResponse) is_flexible() bool {
	return m.version >= 0
}

// write_to encodes m at m.version into w.
pub fn (m &UpdateFeaturesResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &UpdateFeaturesResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.throttle_millis)
	w.write_int16(m.error_code)
	if flexible {
		w.write_compact_nullable_string(m.error_message)
	} else {
		w.write_nullable_string(m.error_message)
	}
	if version <= 1 {
		arr_results := m.results
		if flexible {
			w.write_compact_array_len(arr_results.len)
		} else {
			w.write_array_len(arr_results.len)
		}
		for item_results in arr_results {
			item_results.write_to_versioned(mut w, version, flexible)
		}
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = UpdateFeaturesResponse{version: v}).
pub fn (mut m UpdateFeaturesResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m UpdateFeaturesResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.throttle_millis = r.read_int32()
	m.error_code = r.read_int16()
	if flexible {
		m.error_message = r.read_compact_nullable_string()
	} else {
		m.error_message = r.read_nullable_string()
	}
	if version <= 1 {
		if flexible {
			arr_len = r.read_compact_array_len()
		} else {
			arr_len = r.read_array_len()
		}
		mut arr_results := []UpdateFeaturesResponseResult{cap: if arr_len > 0 { arr_len } else { 0 }}
		for _ in 0 .. arr_len {
			mut item_results := UpdateFeaturesResponseResult{}
			item_results.read_from_versioned(mut r, version, flexible)!
			arr_results << item_results
		}
		m.results = arr_results
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
