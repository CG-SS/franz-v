// Code generated by `v run generate/` from
// franz-go generate/definitions/29_describe_acls — DO NOT EDIT.
module kmsg

import kbin

pub struct DescribeACLsRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version               i16
	resource_type         ACLResourceType
	resource_name         ?string
	resource_pattern_type ACLResourcePatternType = ACLResourcePatternType(3)
	principal             ?string
	host                  ?string
	operation             ACLOperation
	permission_type       ACLPermissionType
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeACLsRequest (29).
pub fn (m &DescribeACLsRequest) key() i16 {
	return 29
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeACLsRequest) max_version() i16 {
	return 3
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeACLsRequest) is_flexible() bool {
	return m.version >= 2
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeACLsRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeACLsRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int8(i8(m.resource_type))
	if flexible {
		w.write_compact_nullable_string(m.resource_name)
	} else {
		w.write_nullable_string(m.resource_name)
	}
	if version >= 1 {
		w.write_int8(i8(m.resource_pattern_type))
	}
	if flexible {
		w.write_compact_nullable_string(m.principal)
	} else {
		w.write_nullable_string(m.principal)
	}
	if flexible {
		w.write_compact_nullable_string(m.host)
	} else {
		w.write_nullable_string(m.host)
	}
	w.write_int8(i8(m.operation))
	w.write_int8(i8(m.permission_type))
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeACLsRequest{version: v}).
pub fn (mut m DescribeACLsRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeACLsRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	m.resource_type = ACLResourceType(r.read_int8())
	if flexible {
		m.resource_name = r.read_compact_nullable_string()
	} else {
		m.resource_name = r.read_nullable_string()
	}
	if version >= 1 {
		m.resource_pattern_type = ACLResourcePatternType(r.read_int8())
	}
	if flexible {
		m.principal = r.read_compact_nullable_string()
	} else {
		m.principal = r.read_nullable_string()
	}
	if flexible {
		m.host = r.read_compact_nullable_string()
	} else {
		m.host = r.read_nullable_string()
	}
	m.operation = ACLOperation(r.read_int8())
	m.permission_type = ACLPermissionType(r.read_int8())
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeACLsResponseResourceACL {
pub mut:
	principal       string
	host            string
	operation       ACLOperation
	permission_type ACLPermissionType
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeACLsResponseResourceACL) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.principal)
	} else {
		w.write_string(m.principal)
	}
	if flexible {
		w.write_compact_string(m.host)
	} else {
		w.write_string(m.host)
	}
	w.write_int8(i8(m.operation))
	w.write_int8(i8(m.permission_type))
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeACLsResponseResourceACL) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.principal = r.read_compact_string()
	} else {
		m.principal = r.read_string()
	}
	if flexible {
		m.host = r.read_compact_string()
	} else {
		m.host = r.read_string()
	}
	m.operation = ACLOperation(r.read_int8())
	m.permission_type = ACLPermissionType(r.read_int8())
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeACLsResponseResource {
pub mut:
	resource_type         ACLResourceType
	resource_name         string
	resource_pattern_type ACLResourcePatternType = ACLResourcePatternType(3)
	ac_ls                 []DescribeACLsResponseResourceACL
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeACLsResponseResource) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int8(i8(m.resource_type))
	if flexible {
		w.write_compact_string(m.resource_name)
	} else {
		w.write_string(m.resource_name)
	}
	if version >= 1 {
		w.write_int8(i8(m.resource_pattern_type))
	}
	arr_ac_ls := m.ac_ls
	if flexible {
		w.write_compact_array_len(arr_ac_ls.len)
	} else {
		w.write_array_len(arr_ac_ls.len)
	}
	for item_ac_ls in arr_ac_ls {
		item_ac_ls.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeACLsResponseResource) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.resource_type = ACLResourceType(r.read_int8())
	if flexible {
		m.resource_name = r.read_compact_string()
	} else {
		m.resource_name = r.read_string()
	}
	if version >= 1 {
		m.resource_pattern_type = ACLResourcePatternType(r.read_int8())
	}
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_ac_ls := []DescribeACLsResponseResourceACL{cap: if arr_len > 0 { arr_len } else { 0 }}
	for _ in 0 .. arr_len {
		mut item_ac_ls := DescribeACLsResponseResourceACL{}
		item_ac_ls.read_from_versioned(mut r, version, flexible)!
		arr_ac_ls << item_ac_ls
	}
	m.ac_ls = arr_ac_ls
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeACLsResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version         i16
	throttle_millis int
	error_code      i16
	error_message   ?string
	resources       []DescribeACLsResponseResource
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeACLsResponse (29).
pub fn (m &DescribeACLsResponse) key() i16 {
	return 29
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeACLsResponse) max_version() i16 {
	return 3
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeACLsResponse) is_flexible() bool {
	return m.version >= 2
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeACLsResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeACLsResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.throttle_millis)
	w.write_int16(m.error_code)
	if flexible {
		w.write_compact_nullable_string(m.error_message)
	} else {
		w.write_nullable_string(m.error_message)
	}
	arr_resources := m.resources
	if flexible {
		w.write_compact_array_len(arr_resources.len)
	} else {
		w.write_array_len(arr_resources.len)
	}
	for item_resources in arr_resources {
		item_resources.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeACLsResponse{version: v}).
pub fn (mut m DescribeACLsResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeACLsResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.throttle_millis = r.read_int32()
	m.error_code = r.read_int16()
	if flexible {
		m.error_message = r.read_compact_nullable_string()
	} else {
		m.error_message = r.read_nullable_string()
	}
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_resources := []DescribeACLsResponseResource{cap: if arr_len > 0 { arr_len } else { 0 }}
	for _ in 0 .. arr_len {
		mut item_resources := DescribeACLsResponseResource{}
		item_resources.read_from_versioned(mut r, version, flexible)!
		arr_resources << item_resources
	}
	m.resources = arr_resources
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
