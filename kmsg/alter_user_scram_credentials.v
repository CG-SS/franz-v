// Code generated by `v run generate/` from
// franz-go generate/definitions/51_alter_user_scram_credentials — DO NOT EDIT.
module kmsg

import kbin

pub struct AlterUserSCRAMCredentialsRequestDeletion {
pub mut:
	name      string
	mechanism i8
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &AlterUserSCRAMCredentialsRequestDeletion) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.name)
	} else {
		w.write_string(m.name)
	}
	w.write_int8(m.mechanism)
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m AlterUserSCRAMCredentialsRequestDeletion) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.name = r.read_compact_string()
	} else {
		m.name = r.read_string()
	}
	m.mechanism = r.read_int8()
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct AlterUserSCRAMCredentialsRequestUpsertion {
pub mut:
	name            string
	mechanism       i8
	iterations      int
	salt            []u8
	salted_password []u8
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &AlterUserSCRAMCredentialsRequestUpsertion) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.name)
	} else {
		w.write_string(m.name)
	}
	w.write_int8(m.mechanism)
	w.write_int32(m.iterations)
	if flexible {
		w.write_compact_bytes(m.salt)
	} else {
		w.write_bytes(m.salt)
	}
	if flexible {
		w.write_compact_bytes(m.salted_password)
	} else {
		w.write_bytes(m.salted_password)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m AlterUserSCRAMCredentialsRequestUpsertion) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.name = r.read_compact_string()
	} else {
		m.name = r.read_string()
	}
	m.mechanism = r.read_int8()
	m.iterations = r.read_int32()
	if flexible {
		m.salt = r.read_compact_bytes()
	} else {
		m.salt = r.read_bytes()
	}
	if flexible {
		m.salted_password = r.read_compact_bytes()
	} else {
		m.salted_password = r.read_bytes()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct AlterUserSCRAMCredentialsRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version    i16
	deletions  []AlterUserSCRAMCredentialsRequestDeletion
	upsertions []AlterUserSCRAMCredentialsRequestUpsertion
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of AlterUserSCRAMCredentialsRequest (51).
pub fn (m &AlterUserSCRAMCredentialsRequest) key() i16 {
	return 51
}

// max_version returns the maximum wire version this client supports.
pub fn (m &AlterUserSCRAMCredentialsRequest) max_version() i16 {
	return 0
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &AlterUserSCRAMCredentialsRequest) is_flexible() bool {
	return m.version >= 0
}

// write_to encodes m at m.version into w.
pub fn (m &AlterUserSCRAMCredentialsRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &AlterUserSCRAMCredentialsRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	arr_deletions := m.deletions
	if flexible {
		w.write_compact_array_len(arr_deletions.len)
	} else {
		w.write_array_len(arr_deletions.len)
	}
	for item_deletions in arr_deletions {
		item_deletions.write_to_versioned(mut w, version, flexible)
	}
	arr_upsertions := m.upsertions
	if flexible {
		w.write_compact_array_len(arr_upsertions.len)
	} else {
		w.write_array_len(arr_upsertions.len)
	}
	for item_upsertions in arr_upsertions {
		item_upsertions.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = AlterUserSCRAMCredentialsRequest{version: v}).
pub fn (mut m AlterUserSCRAMCredentialsRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m AlterUserSCRAMCredentialsRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_deletions := []AlterUserSCRAMCredentialsRequestDeletion{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_deletions := AlterUserSCRAMCredentialsRequestDeletion{}
		item_deletions.read_from_versioned(mut r, version, flexible)!
		arr_deletions << item_deletions
	}
	m.deletions = arr_deletions
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_upsertions := []AlterUserSCRAMCredentialsRequestUpsertion{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_upsertions := AlterUserSCRAMCredentialsRequestUpsertion{}
		item_upsertions.read_from_versioned(mut r, version, flexible)!
		arr_upsertions << item_upsertions
	}
	m.upsertions = arr_upsertions
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct AlterUserSCRAMCredentialsResponseResult {
pub mut:
	user          string
	error_code    i16
	error_message ?string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &AlterUserSCRAMCredentialsResponseResult) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.user)
	} else {
		w.write_string(m.user)
	}
	w.write_int16(m.error_code)
	if flexible {
		w.write_compact_nullable_string(m.error_message)
	} else {
		w.write_nullable_string(m.error_message)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m AlterUserSCRAMCredentialsResponseResult) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.user = r.read_compact_string()
	} else {
		m.user = r.read_string()
	}
	m.error_code = r.read_int16()
	if flexible {
		m.error_message = r.read_compact_nullable_string()
	} else {
		m.error_message = r.read_nullable_string()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct AlterUserSCRAMCredentialsResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version         i16
	throttle_millis int
	results         []AlterUserSCRAMCredentialsResponseResult
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of AlterUserSCRAMCredentialsResponse (51).
pub fn (m &AlterUserSCRAMCredentialsResponse) key() i16 {
	return 51
}

// max_version returns the maximum wire version this client supports.
pub fn (m &AlterUserSCRAMCredentialsResponse) max_version() i16 {
	return 0
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &AlterUserSCRAMCredentialsResponse) is_flexible() bool {
	return m.version >= 0
}

// write_to encodes m at m.version into w.
pub fn (m &AlterUserSCRAMCredentialsResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &AlterUserSCRAMCredentialsResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.throttle_millis)
	arr_results := m.results
	if flexible {
		w.write_compact_array_len(arr_results.len)
	} else {
		w.write_array_len(arr_results.len)
	}
	for item_results in arr_results {
		item_results.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = AlterUserSCRAMCredentialsResponse{version: v}).
pub fn (mut m AlterUserSCRAMCredentialsResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m AlterUserSCRAMCredentialsResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.throttle_millis = r.read_int32()
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_results := []AlterUserSCRAMCredentialsResponseResult{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_results := AlterUserSCRAMCredentialsResponseResult{}
		item_results.read_from_versioned(mut r, version, flexible)!
		arr_results << item_results
	}
	m.results = arr_results
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
