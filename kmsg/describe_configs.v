// Code generated by `v run generate/` from
// franz-go generate/definitions/32_describe_configs — DO NOT EDIT.
module kmsg

import kbin

pub struct DescribeConfigsRequestResource {
pub mut:
	resource_type ConfigResourceType
	resource_name string
	config_names  ?[]string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeConfigsRequestResource) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int8(i8(m.resource_type))
	if flexible {
		w.write_compact_string(m.resource_name)
	} else {
		w.write_string(m.resource_name)
	}
	if arr_config_names := m.config_names {
		if flexible {
			w.write_compact_array_len(arr_config_names.len)
		} else {
			w.write_array_len(arr_config_names.len)
		}
		for item_config_names in arr_config_names {
			if flexible {
				w.write_compact_string(item_config_names)
			} else {
				w.write_string(item_config_names)
			}
		}
	} else {
		if flexible {
			w.write_uvarint(0)
		} else {
			w.write_int32(-1)
		}
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeConfigsRequestResource) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.resource_type = ConfigResourceType(r.read_int8())
	if flexible {
		m.resource_name = r.read_compact_string()
	} else {
		m.resource_name = r.read_string()
	}
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	if arr_len < 0 {
		m.config_names = none
	} else {
		mut arr_config_names := []string{cap: if arr_len > 0 { arr_len } else { 0 }}
		for _ in 0 .. arr_len {
			mut item_config_names := ''
			if flexible {
				item_config_names = r.read_compact_string()
			} else {
				item_config_names = r.read_string()
			}
			arr_config_names << item_config_names
		}
		m.config_names = arr_config_names
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeConfigsRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version               i16
	resources             []DescribeConfigsRequestResource
	include_synonyms      bool
	include_documentation bool
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeConfigsRequest (32).
pub fn (m &DescribeConfigsRequest) key() i16 {
	return 32
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeConfigsRequest) max_version() i16 {
	return 4
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeConfigsRequest) is_flexible() bool {
	return m.version >= 4
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeConfigsRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeConfigsRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	arr_resources := m.resources
	if flexible {
		w.write_compact_array_len(arr_resources.len)
	} else {
		w.write_array_len(arr_resources.len)
	}
	for item_resources in arr_resources {
		item_resources.write_to_versioned(mut w, version, flexible)
	}
	if version >= 1 {
		w.write_bool(m.include_synonyms)
	}
	if version >= 3 {
		w.write_bool(m.include_documentation)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeConfigsRequest{version: v}).
pub fn (mut m DescribeConfigsRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeConfigsRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_resources := []DescribeConfigsRequestResource{cap: if arr_len > 0 { arr_len } else { 0 }}
	for _ in 0 .. arr_len {
		mut item_resources := DescribeConfigsRequestResource{}
		item_resources.read_from_versioned(mut r, version, flexible)!
		arr_resources << item_resources
	}
	m.resources = arr_resources
	if version >= 1 {
		m.include_synonyms = r.read_bool()
	}
	if version >= 3 {
		m.include_documentation = r.read_bool()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeConfigsResponseResourceConfigConfigSynonym {
pub mut:
	name   string
	value  ?string
	source ConfigSource
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeConfigsResponseResourceConfigConfigSynonym) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.name)
	} else {
		w.write_string(m.name)
	}
	if flexible {
		w.write_compact_nullable_string(m.value)
	} else {
		w.write_nullable_string(m.value)
	}
	w.write_int8(i8(m.source))
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeConfigsResponseResourceConfigConfigSynonym) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.name = r.read_compact_string()
	} else {
		m.name = r.read_string()
	}
	if flexible {
		m.value = r.read_compact_nullable_string()
	} else {
		m.value = r.read_nullable_string()
	}
	m.source = ConfigSource(r.read_int8())
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeConfigsResponseResourceConfig {
pub mut:
	name            string
	value           ?string
	read_only       bool
	is_default      bool
	source          ConfigSource = ConfigSource(-1)
	is_sensitive    bool
	config_synonyms []DescribeConfigsResponseResourceConfigConfigSynonym
	config_type     ConfigType
	documentation   ?string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeConfigsResponseResourceConfig) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.name)
	} else {
		w.write_string(m.name)
	}
	if flexible {
		w.write_compact_nullable_string(m.value)
	} else {
		w.write_nullable_string(m.value)
	}
	w.write_bool(m.read_only)
	if version <= 0 {
		w.write_bool(m.is_default)
	}
	if version >= 1 {
		w.write_int8(i8(m.source))
	}
	w.write_bool(m.is_sensitive)
	if version >= 1 {
		arr_config_synonyms := m.config_synonyms
		if flexible {
			w.write_compact_array_len(arr_config_synonyms.len)
		} else {
			w.write_array_len(arr_config_synonyms.len)
		}
		for item_config_synonyms in arr_config_synonyms {
			item_config_synonyms.write_to_versioned(mut w, version, flexible)
		}
	}
	if version >= 3 {
		w.write_int8(i8(m.config_type))
	}
	if version >= 3 {
		if flexible {
			w.write_compact_nullable_string(m.documentation)
		} else {
			w.write_nullable_string(m.documentation)
		}
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeConfigsResponseResourceConfig) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if flexible {
		m.name = r.read_compact_string()
	} else {
		m.name = r.read_string()
	}
	if flexible {
		m.value = r.read_compact_nullable_string()
	} else {
		m.value = r.read_nullable_string()
	}
	m.read_only = r.read_bool()
	if version <= 0 {
		m.is_default = r.read_bool()
	}
	if version >= 1 {
		m.source = ConfigSource(r.read_int8())
	}
	m.is_sensitive = r.read_bool()
	if version >= 1 {
		if flexible {
			arr_len = r.read_compact_array_len()
		} else {
			arr_len = r.read_array_len()
		}
		mut arr_config_synonyms := []DescribeConfigsResponseResourceConfigConfigSynonym{cap: if arr_len > 0 {
			arr_len
		} else {
			0
		}}
		for _ in 0 .. arr_len {
			mut item_config_synonyms := DescribeConfigsResponseResourceConfigConfigSynonym{}
			item_config_synonyms.read_from_versioned(mut r, version, flexible)!
			arr_config_synonyms << item_config_synonyms
		}
		m.config_synonyms = arr_config_synonyms
	}
	if version >= 3 {
		m.config_type = ConfigType(r.read_int8())
	}
	if version >= 3 {
		if flexible {
			m.documentation = r.read_compact_nullable_string()
		} else {
			m.documentation = r.read_nullable_string()
		}
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeConfigsResponseResource {
pub mut:
	error_code    i16
	error_message ?string
	resource_type ConfigResourceType
	resource_name string
	configs       []DescribeConfigsResponseResourceConfig
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeConfigsResponseResource) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int16(m.error_code)
	if flexible {
		w.write_compact_nullable_string(m.error_message)
	} else {
		w.write_nullable_string(m.error_message)
	}
	w.write_int8(i8(m.resource_type))
	if flexible {
		w.write_compact_string(m.resource_name)
	} else {
		w.write_string(m.resource_name)
	}
	arr_configs := m.configs
	if flexible {
		w.write_compact_array_len(arr_configs.len)
	} else {
		w.write_array_len(arr_configs.len)
	}
	for item_configs in arr_configs {
		item_configs.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeConfigsResponseResource) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.error_code = r.read_int16()
	if flexible {
		m.error_message = r.read_compact_nullable_string()
	} else {
		m.error_message = r.read_nullable_string()
	}
	m.resource_type = ConfigResourceType(r.read_int8())
	if flexible {
		m.resource_name = r.read_compact_string()
	} else {
		m.resource_name = r.read_string()
	}
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_configs := []DescribeConfigsResponseResourceConfig{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_configs := DescribeConfigsResponseResourceConfig{}
		item_configs.read_from_versioned(mut r, version, flexible)!
		arr_configs << item_configs
	}
	m.configs = arr_configs
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeConfigsResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version         i16
	throttle_millis int
	resources       []DescribeConfigsResponseResource
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeConfigsResponse (32).
pub fn (m &DescribeConfigsResponse) key() i16 {
	return 32
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeConfigsResponse) max_version() i16 {
	return 4
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeConfigsResponse) is_flexible() bool {
	return m.version >= 4
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeConfigsResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeConfigsResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.throttle_millis)
	arr_resources := m.resources
	if flexible {
		w.write_compact_array_len(arr_resources.len)
	} else {
		w.write_array_len(arr_resources.len)
	}
	for item_resources in arr_resources {
		item_resources.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeConfigsResponse{version: v}).
pub fn (mut m DescribeConfigsResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeConfigsResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.throttle_millis = r.read_int32()
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_resources := []DescribeConfigsResponseResource{cap: if arr_len > 0 { arr_len } else { 0 }}
	for _ in 0 .. arr_len {
		mut item_resources := DescribeConfigsResponseResource{}
		item_resources.read_from_versioned(mut r, version, flexible)!
		arr_resources << item_resources
	}
	m.resources = arr_resources
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
