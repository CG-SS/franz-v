// Code generated by `v run generate/` from
// franz-go generate/definitions/60_describe_cluster — DO NOT EDIT.
module kmsg

import kbin

pub struct DescribeClusterRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version                               i16
	include_cluster_authorized_operations bool
	endpoint_type                         i8 = i8(1)
	include_fenced_brokers                bool
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeClusterRequest (60).
pub fn (m &DescribeClusterRequest) key() i16 {
	return 60
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeClusterRequest) max_version() i16 {
	return 2
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeClusterRequest) is_flexible() bool {
	return m.version >= 0
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeClusterRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeClusterRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_bool(m.include_cluster_authorized_operations)
	if version >= 1 {
		w.write_int8(m.endpoint_type)
	}
	if version >= 2 {
		w.write_bool(m.include_fenced_brokers)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeClusterRequest{version: v}).
pub fn (mut m DescribeClusterRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeClusterRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	m.include_cluster_authorized_operations = r.read_bool()
	if version >= 1 {
		m.endpoint_type = r.read_int8()
	}
	if version >= 2 {
		m.include_fenced_brokers = r.read_bool()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeClusterResponseBroker {
pub mut:
	node_id   int
	host      string
	port      int
	rack      ?string
	is_fenced bool
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeClusterResponseBroker) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.node_id)
	if flexible {
		w.write_compact_string(m.host)
	} else {
		w.write_string(m.host)
	}
	w.write_int32(m.port)
	if flexible {
		w.write_compact_nullable_string(m.rack)
	} else {
		w.write_nullable_string(m.rack)
	}
	if version >= 2 {
		w.write_bool(m.is_fenced)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeClusterResponseBroker) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	m.node_id = r.read_int32()
	if flexible {
		m.host = r.read_compact_string()
	} else {
		m.host = r.read_string()
	}
	m.port = r.read_int32()
	if flexible {
		m.rack = r.read_compact_nullable_string()
	} else {
		m.rack = r.read_nullable_string()
	}
	if version >= 2 {
		m.is_fenced = r.read_bool()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeClusterResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version                       i16
	throttle_millis               int
	error_code                    i16
	error_message                 ?string
	endpoint_type                 i8 = i8(1)
	cluster_id                    string
	controller_id                 int = -1
	brokers                       []DescribeClusterResponseBroker
	cluster_authorized_operations int = -2147483648
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeClusterResponse (60).
pub fn (m &DescribeClusterResponse) key() i16 {
	return 60
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeClusterResponse) max_version() i16 {
	return 2
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeClusterResponse) is_flexible() bool {
	return m.version >= 0
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeClusterResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeClusterResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int32(m.throttle_millis)
	w.write_int16(m.error_code)
	if flexible {
		w.write_compact_nullable_string(m.error_message)
	} else {
		w.write_nullable_string(m.error_message)
	}
	if version >= 1 {
		w.write_int8(m.endpoint_type)
	}
	if flexible {
		w.write_compact_string(m.cluster_id)
	} else {
		w.write_string(m.cluster_id)
	}
	w.write_int32(m.controller_id)
	arr_brokers := m.brokers
	if flexible {
		w.write_compact_array_len(arr_brokers.len)
	} else {
		w.write_array_len(arr_brokers.len)
	}
	for item_brokers in arr_brokers {
		item_brokers.write_to_versioned(mut w, version, flexible)
	}
	w.write_int32(m.cluster_authorized_operations)
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeClusterResponse{version: v}).
pub fn (mut m DescribeClusterResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeClusterResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.throttle_millis = r.read_int32()
	m.error_code = r.read_int16()
	if flexible {
		m.error_message = r.read_compact_nullable_string()
	} else {
		m.error_message = r.read_nullable_string()
	}
	if version >= 1 {
		m.endpoint_type = r.read_int8()
	}
	if flexible {
		m.cluster_id = r.read_compact_string()
	} else {
		m.cluster_id = r.read_string()
	}
	m.controller_id = r.read_int32()
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_brokers := []DescribeClusterResponseBroker{cap: if arr_len > 0 { arr_len } else { 0 }}
	for _ in 0 .. arr_len {
		mut item_brokers := DescribeClusterResponseBroker{}
		item_brokers.read_from_versioned(mut r, version, flexible)!
		arr_brokers << item_brokers
	}
	m.brokers = arr_brokers
	m.cluster_authorized_operations = r.read_int32()
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
