// Code generated by `v run generate/` from
// franz-go generate/definitions/15_describe_groups — DO NOT EDIT.
module kmsg

import kbin

pub struct DescribeGroupsRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version                       i16
	groups                        []string
	include_authorized_operations bool
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeGroupsRequest (15).
pub fn (m &DescribeGroupsRequest) key() i16 {
	return 15
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeGroupsRequest) max_version() i16 {
	return 6
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeGroupsRequest) is_flexible() bool {
	return m.version >= 5
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeGroupsRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeGroupsRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	arr_groups := m.groups
	if flexible {
		w.write_compact_array_len(arr_groups.len)
	} else {
		w.write_array_len(arr_groups.len)
	}
	for item_groups in arr_groups {
		if flexible {
			w.write_compact_string(item_groups)
		} else {
			w.write_string(item_groups)
		}
	}
	if version >= 3 {
		w.write_bool(m.include_authorized_operations)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeGroupsRequest{version: v}).
pub fn (mut m DescribeGroupsRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeGroupsRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_groups := []string{cap: if arr_len > 0 { arr_len } else { 0 }}
	for _ in 0 .. arr_len {
		mut item_groups := ''
		if flexible {
			item_groups = r.read_compact_string()
		} else {
			item_groups = r.read_string()
		}
		arr_groups << item_groups
	}
	m.groups = arr_groups
	if version >= 3 {
		m.include_authorized_operations = r.read_bool()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeGroupsResponseGroupMember {
pub mut:
	member_id         string
	instance_id       ?string
	client_id         string
	client_host       string
	protocol_metadata []u8
	member_assignment []u8
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeGroupsResponseGroupMember) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.member_id)
	} else {
		w.write_string(m.member_id)
	}
	if version >= 4 {
		if flexible {
			w.write_compact_nullable_string(m.instance_id)
		} else {
			w.write_nullable_string(m.instance_id)
		}
	}
	if flexible {
		w.write_compact_string(m.client_id)
	} else {
		w.write_string(m.client_id)
	}
	if flexible {
		w.write_compact_string(m.client_host)
	} else {
		w.write_string(m.client_host)
	}
	if flexible {
		w.write_compact_bytes(m.protocol_metadata)
	} else {
		w.write_bytes(m.protocol_metadata)
	}
	if flexible {
		w.write_compact_bytes(m.member_assignment)
	} else {
		w.write_bytes(m.member_assignment)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeGroupsResponseGroupMember) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.member_id = r.read_compact_string()
	} else {
		m.member_id = r.read_string()
	}
	if version >= 4 {
		if flexible {
			m.instance_id = r.read_compact_nullable_string()
		} else {
			m.instance_id = r.read_nullable_string()
		}
	}
	if flexible {
		m.client_id = r.read_compact_string()
	} else {
		m.client_id = r.read_string()
	}
	if flexible {
		m.client_host = r.read_compact_string()
	} else {
		m.client_host = r.read_string()
	}
	if flexible {
		m.protocol_metadata = r.read_compact_bytes()
	} else {
		m.protocol_metadata = r.read_bytes()
	}
	if flexible {
		m.member_assignment = r.read_compact_bytes()
	} else {
		m.member_assignment = r.read_bytes()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeGroupsResponseGroup {
pub mut:
	error_code            i16
	error_message         ?string
	group                 string
	state                 string
	protocol_type         string
	protocol              string
	members               []DescribeGroupsResponseGroupMember
	authorized_operations int = -2147483648
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeGroupsResponseGroup) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int16(m.error_code)
	if version >= 6 {
		if flexible {
			w.write_compact_nullable_string(m.error_message)
		} else {
			w.write_nullable_string(m.error_message)
		}
	}
	if flexible {
		w.write_compact_string(m.group)
	} else {
		w.write_string(m.group)
	}
	if flexible {
		w.write_compact_string(m.state)
	} else {
		w.write_string(m.state)
	}
	if flexible {
		w.write_compact_string(m.protocol_type)
	} else {
		w.write_string(m.protocol_type)
	}
	if flexible {
		w.write_compact_string(m.protocol)
	} else {
		w.write_string(m.protocol)
	}
	arr_members := m.members
	if flexible {
		w.write_compact_array_len(arr_members.len)
	} else {
		w.write_array_len(arr_members.len)
	}
	for item_members in arr_members {
		item_members.write_to_versioned(mut w, version, flexible)
	}
	if version >= 3 {
		w.write_int32(m.authorized_operations)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeGroupsResponseGroup) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.error_code = r.read_int16()
	if version >= 6 {
		if flexible {
			m.error_message = r.read_compact_nullable_string()
		} else {
			m.error_message = r.read_nullable_string()
		}
	}
	if flexible {
		m.group = r.read_compact_string()
	} else {
		m.group = r.read_string()
	}
	if flexible {
		m.state = r.read_compact_string()
	} else {
		m.state = r.read_string()
	}
	if flexible {
		m.protocol_type = r.read_compact_string()
	} else {
		m.protocol_type = r.read_string()
	}
	if flexible {
		m.protocol = r.read_compact_string()
	} else {
		m.protocol = r.read_string()
	}
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_members := []DescribeGroupsResponseGroupMember{cap: if arr_len > 0 { arr_len } else { 0 }}
	for _ in 0 .. arr_len {
		mut item_members := DescribeGroupsResponseGroupMember{}
		item_members.read_from_versioned(mut r, version, flexible)!
		arr_members << item_members
	}
	m.members = arr_members
	if version >= 3 {
		m.authorized_operations = r.read_int32()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeGroupsResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version         i16
	throttle_millis int
	groups          []DescribeGroupsResponseGroup
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeGroupsResponse (15).
pub fn (m &DescribeGroupsResponse) key() i16 {
	return 15
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeGroupsResponse) max_version() i16 {
	return 6
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeGroupsResponse) is_flexible() bool {
	return m.version >= 5
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeGroupsResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeGroupsResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if version >= 1 {
		w.write_int32(m.throttle_millis)
	}
	arr_groups := m.groups
	if flexible {
		w.write_compact_array_len(arr_groups.len)
	} else {
		w.write_array_len(arr_groups.len)
	}
	for item_groups in arr_groups {
		item_groups.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeGroupsResponse{version: v}).
pub fn (mut m DescribeGroupsResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeGroupsResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if version >= 1 {
		m.throttle_millis = r.read_int32()
	}
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_groups := []DescribeGroupsResponseGroup{cap: if arr_len > 0 { arr_len } else { 0 }}
	for _ in 0 .. arr_len {
		mut item_groups := DescribeGroupsResponseGroup{}
		item_groups.read_from_versioned(mut r, version, flexible)!
		arr_groups << item_groups
	}
	m.groups = arr_groups
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
