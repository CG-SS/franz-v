// Code generated by `v run generate/` from
// franz-go generate/definitions/41_describe_delegation_token — DO NOT EDIT.
module kmsg

import kbin

pub struct DescribeDelegationTokenRequestOwner {
pub mut:
	principal_type string
	principal_name string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeDelegationTokenRequestOwner) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.principal_type)
	} else {
		w.write_string(m.principal_type)
	}
	if flexible {
		w.write_compact_string(m.principal_name)
	} else {
		w.write_string(m.principal_name)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeDelegationTokenRequestOwner) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.principal_type = r.read_compact_string()
	} else {
		m.principal_type = r.read_string()
	}
	if flexible {
		m.principal_name = r.read_compact_string()
	} else {
		m.principal_name = r.read_string()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeDelegationTokenRequest {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version i16
	owners  ?[]DescribeDelegationTokenRequestOwner
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeDelegationTokenRequest (41).
pub fn (m &DescribeDelegationTokenRequest) key() i16 {
	return 41
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeDelegationTokenRequest) max_version() i16 {
	return 3
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeDelegationTokenRequest) is_flexible() bool {
	return m.version >= 2
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeDelegationTokenRequest) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeDelegationTokenRequest) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if arr_owners := m.owners {
		if flexible {
			w.write_compact_array_len(arr_owners.len)
		} else {
			w.write_array_len(arr_owners.len)
		}
		for item_owners in arr_owners {
			item_owners.write_to_versioned(mut w, version, flexible)
		}
	} else {
		if flexible {
			w.write_uvarint(0)
		} else {
			w.write_int32(-1)
		}
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeDelegationTokenRequest{version: v}).
pub fn (mut m DescribeDelegationTokenRequest) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeDelegationTokenRequest) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	if arr_len < 0 {
		m.owners = none
	} else {
		mut arr_owners := []DescribeDelegationTokenRequestOwner{cap: if arr_len > 0 {
			arr_len
		} else {
			0
		}}
		for _ in 0 .. arr_len {
			mut item_owners := DescribeDelegationTokenRequestOwner{}
			item_owners.read_from_versioned(mut r, version, flexible)!
			arr_owners << item_owners
		}
		m.owners = arr_owners
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeDelegationTokenResponseTokenDetailRenewer {
pub mut:
	principal_type string
	principal_name string
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeDelegationTokenResponseTokenDetailRenewer) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.principal_type)
	} else {
		w.write_string(m.principal_type)
	}
	if flexible {
		w.write_compact_string(m.principal_name)
	} else {
		w.write_string(m.principal_name)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeDelegationTokenResponseTokenDetailRenewer) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	if flexible {
		m.principal_type = r.read_compact_string()
	} else {
		m.principal_type = r.read_string()
	}
	if flexible {
		m.principal_name = r.read_compact_string()
	} else {
		m.principal_name = r.read_string()
	}
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeDelegationTokenResponseTokenDetail {
pub mut:
	principal_type                 string
	principal_name                 string
	token_requester_principal_type string
	token_requester_principal_name string
	issue_timestamp                i64
	expiry_timestamp               i64
	max_timestamp                  i64
	token_id                       string
	hmac                           []u8
	renewers                       []DescribeDelegationTokenResponseTokenDetailRenewer
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

fn (m &DescribeDelegationTokenResponseTokenDetail) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	if flexible {
		w.write_compact_string(m.principal_type)
	} else {
		w.write_string(m.principal_type)
	}
	if flexible {
		w.write_compact_string(m.principal_name)
	} else {
		w.write_string(m.principal_name)
	}
	if version >= 3 {
		if flexible {
			w.write_compact_string(m.token_requester_principal_type)
		} else {
			w.write_string(m.token_requester_principal_type)
		}
	}
	if version >= 3 {
		if flexible {
			w.write_compact_string(m.token_requester_principal_name)
		} else {
			w.write_string(m.token_requester_principal_name)
		}
	}
	w.write_int64(m.issue_timestamp)
	w.write_int64(m.expiry_timestamp)
	w.write_int64(m.max_timestamp)
	if flexible {
		w.write_compact_string(m.token_id)
	} else {
		w.write_string(m.token_id)
	}
	if flexible {
		w.write_compact_bytes(m.hmac)
	} else {
		w.write_bytes(m.hmac)
	}
	arr_renewers := m.renewers
	if flexible {
		w.write_compact_array_len(arr_renewers.len)
	} else {
		w.write_array_len(arr_renewers.len)
	}
	for item_renewers in arr_renewers {
		item_renewers.write_to_versioned(mut w, version, flexible)
	}
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

fn (mut m DescribeDelegationTokenResponseTokenDetail) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	if flexible {
		m.principal_type = r.read_compact_string()
	} else {
		m.principal_type = r.read_string()
	}
	if flexible {
		m.principal_name = r.read_compact_string()
	} else {
		m.principal_name = r.read_string()
	}
	if version >= 3 {
		if flexible {
			m.token_requester_principal_type = r.read_compact_string()
		} else {
			m.token_requester_principal_type = r.read_string()
		}
	}
	if version >= 3 {
		if flexible {
			m.token_requester_principal_name = r.read_compact_string()
		} else {
			m.token_requester_principal_name = r.read_string()
		}
	}
	m.issue_timestamp = r.read_int64()
	m.expiry_timestamp = r.read_int64()
	m.max_timestamp = r.read_int64()
	if flexible {
		m.token_id = r.read_compact_string()
	} else {
		m.token_id = r.read_string()
	}
	if flexible {
		m.hmac = r.read_compact_bytes()
	} else {
		m.hmac = r.read_bytes()
	}
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_renewers := []DescribeDelegationTokenResponseTokenDetailRenewer{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_renewers := DescribeDelegationTokenResponseTokenDetailRenewer{}
		item_renewers.read_from_versioned(mut r, version, flexible)!
		arr_renewers << item_renewers
	}
	m.renewers = arr_renewers
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}

pub struct DescribeDelegationTokenResponse {
pub mut:
	// version is the wire version to encode with / that was used
	// by the request this response answers. Set it before use.
	version         i16
	error_code      i16
	token_details   []DescribeDelegationTokenResponseTokenDetail
	throttle_millis int
	// unknown_tags preserves tagged fields this client does not know.
	unknown_tags []UnknownTag
}

// key returns the Kafka API key of DescribeDelegationTokenResponse (41).
pub fn (m &DescribeDelegationTokenResponse) key() i16 {
	return 41
}

// max_version returns the maximum wire version this client supports.
pub fn (m &DescribeDelegationTokenResponse) max_version() i16 {
	return 3
}

// is_flexible reports whether m.version uses flexible (compact) encoding.
pub fn (m &DescribeDelegationTokenResponse) is_flexible() bool {
	return m.version >= 2
}

// write_to encodes m at m.version into w.
pub fn (m &DescribeDelegationTokenResponse) write_to(mut w kbin.Writer) {
	version := m.version
	flexible := m.is_flexible()
	m.write_to_versioned(mut w, version, flexible)
}

fn (m &DescribeDelegationTokenResponse) write_to_versioned(mut w kbin.Writer, version i16, flexible bool) {
	_ = version
	_ = flexible
	w.write_int16(m.error_code)
	arr_token_details := m.token_details
	if flexible {
		w.write_compact_array_len(arr_token_details.len)
	} else {
		w.write_array_len(arr_token_details.len)
	}
	for item_token_details in arr_token_details {
		item_token_details.write_to_versioned(mut w, version, flexible)
	}
	w.write_int32(m.throttle_millis)
	if flexible {
		w.write_uvarint(u32(m.unknown_tags.len))
		write_unknown_tags(mut w, m.unknown_tags)
	}
}

// read_from decodes into m using m.version; call on a fresh
// struct (or after reassigning m = DescribeDelegationTokenResponse{version: v}).
pub fn (mut m DescribeDelegationTokenResponse) read_from(mut r kbin.Reader) ! {
	version := m.version
	flexible := m.is_flexible()
	m.read_from_versioned(mut r, version, flexible)!
	r.complete()!
}

fn (mut m DescribeDelegationTokenResponse) read_from_versioned(mut r kbin.Reader, version i16, flexible bool) ! {
	_ = version
	_ = flexible
	mut arr_len := 0
	_ = arr_len
	m.error_code = r.read_int16()
	if flexible {
		arr_len = r.read_compact_array_len()
	} else {
		arr_len = r.read_array_len()
	}
	mut arr_token_details := []DescribeDelegationTokenResponseTokenDetail{cap: if arr_len > 0 {
		arr_len
	} else {
		0
	}}
	for _ in 0 .. arr_len {
		mut item_token_details := DescribeDelegationTokenResponseTokenDetail{}
		item_token_details.read_from_versioned(mut r, version, flexible)!
		arr_token_details << item_token_details
	}
	m.token_details = arr_token_details
	m.throttle_millis = r.read_int32()
	if flexible {
		num_tags := r.read_uvarint()
		for _ in 0 .. num_tags {
			tag := r.read_uvarint()
			size := int(r.read_uvarint())
			data := r.span(size)
			if r.ok() {
				m.unknown_tags << UnknownTag{
					tag:  tag
					data: data.clone()
				}
			}
		}
	}
	r.complete()!
}
